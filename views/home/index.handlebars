<div class="row">
  <div class="col-md-3"></div>
  <div class="col-md-6">
      <form method="post" enctype="application/x-www-form-urlencoded">
        <p>Give your SitRep a name, and we can get it setup pronto!</p>
        <p class="text-danger">
            {{validationMessage}}
        </p>
        <p>
            <span>
                <input id="proposedIdInput" 
                       name="proposedId" 
                       style="width:100%; font-size:1.2em;" 
                       autofocus 
                       required 
                       placeholder="SitRep Name (use only numbers, letters, & spaces)" 
                       pattern="[a-zA-Z0-9\s]+"
                       validationMessage="Please use only numbers, letters, and spaces."/>
            </span>
        </p>
        <p>
           <span>
               <input id="emailInput" name="ownerEmail" style="width:100%; font-size:1.2em;" type="email" required placeholder="somebody@example.com"/>
           </span>
        </p>
        <p>
           <span>
               <input id="passwordInput" name="password" style="width:100%; font-size:1.2em;" type="password" placeholder="password" required min="10" max="100" />
           </span>
        </p>
        <p>
            <input type="submit" class="btn btn-primary" value="Create SitRep" />
        </p>
    </form>
  </div>
  <div class="col-md-3"></div>
</div>